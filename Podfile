# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GifViewer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GifViewer
  #pod 'RxSwift'
  #pod 'RxCocoa'
  pod 'FLAnimatedImage'
  pod 'Firebase/Core'
  pod 'Firebase/AdMob'

  target 'GifViewerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GifViewerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
